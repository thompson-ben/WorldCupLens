# Public base URL, used for canonical links, sitemap and robots.
SITE_URL=http://localhost:3000

# Supabase (optional). When both are set, the app uses SupabaseDataProvider;
# otherwise it falls back to the in-memory seed data.
SUPABASE_URL=
SUPABASE_ANON_KEY=
